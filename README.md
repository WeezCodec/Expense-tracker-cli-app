# Expense-tracker-cli-app

[200~🧱 Features

Add, view, and remove expenses from a list.

Automatically saves data to a file (expenses.txt).

Calculates total and monthly breakdowns.

Uses functions and basic OOP for structure.

Handles invalid input safely.
